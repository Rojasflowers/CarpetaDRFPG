#include <stdio.h>
int main ()
{
    int c,c1,c2;
    printf("ingrese su contraseña");
    scanf("%i %i %i",&c, &c1, &c2);
    if (c=1){
        if(c1=2){
            if(c2=3){
                printf("acceso permitido:)");
            }
        }
    }

    return 0; 
}